import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from '../components/atoms/Button';
import { SearchIcon } from '../components/atoms/Icons';
import { withKnobs, text, boolean, number } from '@storybook/addon-knobs';

<Meta title="Atoms/Button" component={Button} />

# Button

Documentação Atom Button

<Preview>
  <Story name="default">
    <Button>Texto</Button>
  </Story>
</Preview>

> Exemplo do componente sem nenhuma propriedade

<br/>

### Propriedades

<Props of={Button} />

<br/><br/>

## Button with icon

<Preview>
  <Story name="button icon">
    <Button>
      <SearchIcon />
    </Button>
  </Story>
</Preview>

> O componente Button aceita receber icones no corpo do componente.

<br/>

## Button colors

<Preview>
  <Story name="button colors">
    <Button color="#5c5c5c" bgColor="#ccc" size='large' >
      Text Color and button color
    </Button>
  </Story>
</Preview>

> O componente Button aceita receber valores para alterar a cor do texto e a cor de fundo do botão.>

<br/>

## Button size

<Preview>
  <Story name="button size">
    <>
      <Button size='small'>small</Button>
      <br/><br/>
      <Button size='normal'>normal</Button>
      <br/><br/>
      <Button size='large'>large</Button>
    </>f
  </Story>
</Preview>

> O componente Button comporta 3 tamanhos diferentes que são setados atravez
> da propriedade size.
> Os valores disponiveis são `small`, `normal` e `large`.

<br/>

## Button loader

<Preview>
  <Story name="button loader">
    <Button loader={true} >
      Button
    </Button>
  </Story>
</Preview>

<br/>

### Playground
<Story name="knobs" parameters={{ decorators: [withKnobs] }}>
  <Button
    size={text('size')}
    color={text('color')}
    bgColor={text('bgColor')}
    loader={boolean('loader')}
  >Playground</Button>
</Story>
